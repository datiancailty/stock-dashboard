name: 旧版 GitHub live 行情更新（已退休）
# Superseded by the authorized local deterministic Supabase refresh worker.
# Do not re-enable public live data writes or MX/AI searches here.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain private refresh destination
        run: printf '%s\n' 'Retired. Part 1-6 data is refreshed by the local Mac worker into authenticated Supabase RPCs. No data/provider calls performed.'
