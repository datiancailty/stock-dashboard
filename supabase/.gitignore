# Local-only Supabase / VPS secrets. Never commit any of these.
.env
.env.*
!.env.example
*.secret
*.pem
*.key
service-role-*.json
# Supabase CLI link/cache metadata is machine-local; do not version it.
.temp/
